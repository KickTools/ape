/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['static-cdn.jtvnw.net', 'kick.com'], // Adjust based on actual URLs
    },
  };
  
  module.exports = nextConfig;